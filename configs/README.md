# 配置目录

本目录用于后续可复现实验配置。

当前状态：

- WP-01A 已完成，但明确不实现 YAML 配置或配置加载器；
- WP-01B 只实现三类非平稳需求过程，仍不得提前引入持久化配置；
- WP-01C 才允许设计配置文件、工厂扩展、CLI 覆盖和轨迹序列化。

后续配置预计按以下层级组织，但目录和字段只有在 WP-01C 设计审查后才能冻结：

```text
configs/
├── demand/
├── env/
├── forecast/
├── algo/
└── experiment/
```

所有正式实验必须保存解析后的完整配置、随机种子和 Git Commit。大型生成数据、日志和模型不得提交 GitHub。
