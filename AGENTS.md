# FURA-MAPPO Codex 协作规范

本文件适用于仓库根目录及其全部子目录。工作包中的明确约束如比本文件更严格，按更严格的约束执行。

## 开发环境与阶段约束

1. 项目统一使用 Python 3.11。
2. A100 服务器的系统 Python 2.7 不得用于本项目。
3. WP-01 阶段不得引入 PyTorch。
4. 大型训练、多随机种子实验和 GPU 任务只能由用户在服务器启动；Codex 不得启动这些任务。
5. 代码是否可合并，以 Mac 测试、服务器测试和 GitHub Actions 的实际结果为准。

## 代码规范

1. 文件名、模块名、类名、函数名和变量名使用英文。
2. 模块说明、Docstring 和关键算法注释使用中文。
3. 所有公共函数和方法必须有类型标注。
4. NumPy 数组和未来的 PyTorch 张量必须在 Docstring 中注明形状。
5. 所有随机过程必须使用独立的 `numpy.random.Generator`。
6. 不得依赖或修改 `numpy.random` 全局随机状态。
7. 不得使用隐藏的全局可变状态。

## 修改边界与真实性

1. 不得删除、跳过或放宽已有测试来绕过错误。
2. 不得虚构测试结果、性能结果或已执行命令；无法执行的测试必须明确报告。
3. 不得提前实现当前工作包以外的功能。
4. 不得进行与任务无关的大规模重构。
5. 不得修改冻结的公共接口，除非工作包明确授权。
6. 不得提交 artifacts、日志、数据集、模型、Token、SSH 配置或服务器敏感信息。

## Git 与交付

1. 项目采用 main-only 流程；Codex 只在用户指定的当前 `main` 工作目录中修改和测试文件。
2. Codex 不得执行 Commit、Tag、Push 或任何分支操作，也不得创建额外 worktree 或 Pull Request。
3. 完成任务后，Codex 必须报告 `git diff` 的摘要和 `git status --short`。
4. 完成前必须运行工作包规定的 Ruff 和 Pytest 命令；无法运行或失败时如实报告，不得擅自改变环境或依赖。
5. 版本管理和服务器验收流程遵循 `docs/CODEX_WORKFLOW.md`。
