# FURA-MAPPO 工作包模板

## 1. 基本信息

- 工作包编号：`<WP-XX>`
- 工作包名称：`<名称>`
- 科学目标或工程目的：`<问题或假设>`
- 远程基线 Commit：`<完整 SHA>`
- 当前分支：`main`
- 当前稳定标签：`<如有；非必需>`
- 工作树初始状态：`<clean 或明确差异>`

## 2. 唯一目标

`<用一句可验收的话描述本工作包唯一目标。>`

## 3. 范围

### 允许新增或修改

- `<路径>`

### 禁止修改

- `<路径或接口>`

### 非目标

- `<明确不实现内容>`

## 4. 要求

### 功能与接口

- `<公共类、函数、签名、输入和输出>`
- `<状态语义和异常>`

### 科学约束

- `<外生性、统计假设、随机性和可复现性>`
- `<数组形状、dtype、值域和单位>`

### 配置和数据安全

- `<不修改输入、无可变默认值、拒绝未知字段等>`

### 测试

- `<必须新增的单元、边界、状态、随机和统计测试>`
- `<原有测试必须保持通过>`
- `<禁止跳过、删除或放宽的测试>`

## 5. Codex 执行

### 只读分析

- 当前 Commit、分支和工作树；
- 现有结构和相关工具；
- 推荐接口和文件；
- 风险和待决策项。

### 实现限制

- 不 Commit、Push、Tag 或操作分支；
- 不安装依赖或修改环境；
- 不扩大文件范围；
- 如实报告命令和结果。

### Mac 快速测试

```bash
<专项命令>
<scripts/verify_cpu.sh>
```

## 6. 完整补丁审查

- 补丁文件名：`<wp>-review.patch`
- 生成目录：`$HOME/Downloads/` 或 `$HOME/Desktop/`
- 禁止目录：`/tmp`
- 必须包含：已跟踪修改和相关未跟踪新文件
- 补丁 SHA-256：`<值>`
- Diff 段数：`<值>`

独立审查记录：

- 应用检查：
- 代码审查：
- 专项测试：
- 完整测试：
- 环境差异：
- 阻断问题：
- 结论：`<修复后重审/可进入 Mac 最终验收>`

## 7. 正式验收

### Mac 最终验收

```bash
git diff --check
git status --short
<正式 Mac 命令>
```

### Commit 和 Push

- 明确暂存文件：`<列表>`
- Commit message：`<消息>`
- 由用户手工执行；禁止 force push。

### A100 服务器验收

```bash
git pull --ff-only origin main
python -m pip install -e ".[dev]"
<正式服务器命令>
```

### GitHub Actions

- 作为被动附加检查记录实际结果；
- 不是服务器开始验收的前置条件。

## 8. 完成标准

- [ ] 唯一目标已实现；
- [ ] 修改范围正确；
- [ ] 完整补丁独立审查通过；
- [ ] Mac 验收通过；
- [ ] 用户已 Commit 和 Push；
- [ ] A100 服务器验收通过；
- [ ] GitHub Actions 结果已记录；
- [ ] 项目状态、决策和会话交接已更新；
- [ ] 未提交补丁、ZIP、日志、缓存、数据或模型。

## 9. 交接

- 实际 Commit：
- 已执行命令和真实结果：
- 未执行或失败检查：
- 已知风险：
- 下一唯一目标：
- 需要下一会话读取的文档：
