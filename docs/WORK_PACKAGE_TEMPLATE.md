# FURA-MAPPO 工作包模板

## 基本信息

- 工作包编号：`<WP-XX>`
- 工作包名称：`<名称>`
- 科学目标：`<该工作包要验证的科学问题或假设>`
- 基线 Commit：`<完整 SHA>`
- 基线稳定标签：`<stable 标签>`
- 回退稳定标签：`<发生问题时使用的已验收标签>`

## 唯一目标

`<用一句可验收的话描述本工作包的唯一目标。>`

## 修改范围

### 允许修改的文件

- `<路径或 glob>`

### 禁止修改的文件

- `<路径或 glob>`

## 要求与约束

### 功能要求

1. `<功能要求>`

### 公共接口

- `<新增、冻结或获准修改的函数、类、数据结构及签名>`
- `<输入、输出、异常和兼容性要求>`

### 科学约束

- `<随机性、统计假设、数值范围、可复现性等约束>`
- `<数组或张量形状约定>`

### 测试要求

- `<必须新增或保持通过的单元测试、统计测试和边界测试>`
- `<禁止删除、跳过或放宽的测试>`

### 非目标

- `<本工作包明确不实现的内容>`

## 验收

### Mac 验收命令

```bash
<快速 CPU 验收命令>
```

### A100 服务器验收命令

```bash
<服务器 CPU/GPU/大规模验收命令；由用户启动>
```

### 预期输出

- `<每条命令的关键预期结果、容差或产物>`

### 完成标准

- [ ] 唯一目标已实现。
- [ ] 修改仅发生在允许范围内。
- [ ] Mac 验收通过。
- [ ] candidate 标签对应 Commit 的 A100 服务器验收通过。
- [ ] `main` 推送后的 GitHub Actions 通过。
- [ ] 稳定标签已由用户创建并推送。

## 风险与交接

### 已知风险

- `<风险、影响和缓解措施>`

### 会话交接信息

- 当前 Commit：`<完整 SHA>`
- 当前标签：`<candidate 或 stable 标签>`
- 已完成内容：`<摘要>`
- 已执行命令及实际结果：`<不得虚构>`
- 未执行或失败的检查：`<命令、原因、错误摘要>`
- 待处理事项：`<下一步>`
- 服务器脱敏日志位置或摘要：`<不提交大型日志或敏感信息>`
