# 安全与隐私

## 禁止提交的内容

本研究仓库不得提交：

- SSH 私钥、公钥口令或服务器密码；
- GitHub Token、云服务密钥、API 密钥或 `.env` 凭据；
- 服务器公网 IP、内部网络拓扑或不必要的完整主机名；
- 未经人工检查的系统审计原始输出；
- 含个人身份信息的绝对路径；
- 大型训练数据、生成轨迹、检查点、模型和日志；
- ChatGPT 审查补丁、ZIP 压缩包或临时导出文件。

## 审查文件

面向人工传输的补丁和文档压缩包只放在用户的 Desktop、Downloads 或会话下载目录，不使用 `/tmp`。这些文件只用于审查和合并，不应进入 Git 暂存区。

提交前至少检查：

```bash
git status --short
git diff --cached
```

必要时搜索 Token、私钥头、绝对用户路径和大文件。

## 凭据泄露处理

发现凭据误提交时，不应只删除文件再提交。必须立即：

1. 吊销或轮换凭据；
2. 停止继续使用受影响凭据；
3. 评估远程历史和日志暴露范围；
4. 使用经过确认的历史清理方案移除敏感内容；
5. 通知相关账户或服务所有者。

项目一般禁止重写已推送 `main` 历史，但真实凭据泄露属于安全事件，应优先完成凭据吊销，并在明确评估后采取必要的历史清理措施。
