# 安全与隐私

本研究仓库不得提交：

- SSH私钥、公钥口令或服务器密码；
- GitHub Token、云服务密钥或API密钥；
- `.env`中的凭据；
- 服务器公网IP、内部网络拓扑或不必要的完整主机名；
- 未经检查的系统审计原始输出；
- 含个人身份信息的绝对路径；
- 大型训练数据、检查点和日志。

发现凭据误提交时，不应只删除文件并再次提交；应立即吊销凭据，并从Git历史中清除相关内容。
